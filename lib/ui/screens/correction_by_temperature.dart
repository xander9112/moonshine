import 'package:flutter/material.dart';

class CorrectionByTemperature extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return SafeArea(child: Center(child: Text('CorrectionByTemperature')));
  }
}
