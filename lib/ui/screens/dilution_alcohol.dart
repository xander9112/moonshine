import 'package:flutter/material.dart';

class DilutionAlcohol extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return SafeArea(child: Center(child: Text('DilutionAlcohol')));
  }
}
