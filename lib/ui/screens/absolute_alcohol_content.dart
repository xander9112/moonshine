import 'package:flutter/material.dart';

class AbsoluteAlcoholContent extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return SafeArea(child: Center(child: Text('AbsoluteAlcoholContent')));
  }
}
